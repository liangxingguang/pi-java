/**
 * Remote session client for pi-java.
 *
 * <p>Connects to a pi-java-server instance over the CBOR protocol.
 * Full implementation in Phase 6.</p>
 */
module com.pijava.client {
    requires com.pijava.protocol;

    // exports com.pijava.client; — Phase 6
}
