/**
 * SQLite-backed session storage.
 *
 * <p>Phase 4 will implement {@link com.pijava.agent.session.SessionStorage}
 * and {@link com.pijava.agent.session.SessionRepository} using SQLite with
 * JSONL v4 format, FTS5 search, writer leases, and branch caching.</p>
 */
package com.pijava.session.backend.sqlite;
