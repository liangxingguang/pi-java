<?xml version="1.0" encoding="UTF-8"?>
<!--
  Bundled default logging config (10-logging-design §5).

  Root level is INFO; the debug flag raises it to DEBUG at runtime (Logging.configure).
  The CONSOLE appender writes to stderr so it never pollutes stdout / print-mode
  output. In interactive (TUI) mode Logging.configure detaches CONSOLE so raw-mode
  alternate-screen rendering is never corrupted; the FILE appender stays active.

  A user override at ~/.pi-java/logback.xml takes precedence when present.
-->
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${user.home}/.pi-java/logs/pi-java.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${user.home}/.pi-java/logs/pi-java.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>5</maxHistory>
            <totalSizeCap>50MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
