<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">
<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="warning"/>

    <property name="fileExtensions" value="java, properties, xml"/>

    <!-- Line length 120 columns -->
    <module name="LineLength">
        <property name="max" value="120"/>
        <property name="ignorePattern" value="^package.*|^import.*"/>
    </module>

    <!-- File length 500 lines (matches CLAUDE.md convention) -->
    <module name="FileLength">
        <property name="max" value="500"/>
    </module>

    <!-- File must end with a newline -->
    <module name="NewlineAtEndOfFile"/>

    <module name="TreeWalker">
        <!-- No star imports -->
        <module name="AvoidStarImport"/>

        <!-- No unused imports -->
        <module name="UnusedImports"/>

        <!-- No empty catch blocks (unless variable is named "expected" or "ignored") -->
        <module name="EmptyCatchBlock">
            <property name="exceptionVariableName" value="expected|ignored"/>
        </module>

        <!-- No System.out / System.err (use java.util.logging) -->
        <module name="RegexpSinglelineJava">
            <property name="format" value="System\.(out|err)\.print"/>
            <property name="message"
                      value="Use java.util.logging.Logger instead of System.out/err"/>
        </module>

        <!-- Javadoc required on public methods -->
        <module name="MissingJavadocMethod">
            <property name="scope" value="public"/>
            <property name="allowMissingPropertyJavadoc" value="true"/>
        </module>

        <!-- No trailing whitespace -->
        <module name="RegexpSinglelineJava">
            <property name="format" value="\s+$"/>
            <property name="message" value="Line has trailing whitespace"/>
        </module>
    </module>
</module>
