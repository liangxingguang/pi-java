name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Checkstyle annotation upload needs checks:write; default read-only GITHUB_TOKEN
# would otherwise fail with "Resource not accessible by integration".
permissions:
  checks: write
  contents: read

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
      fail-fast: false

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: 'maven'

      - name: Verify
        run: ./mvnw clean verify --batch-mode

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.os }}
          path: '**/target/surefire-reports/*.xml'

      - name: Checkstyle report
        if: always()
        uses: jwgmeligmeyling/checkstyle-github-action@v1
        with:
          path: '**/target/checkstyle-result.xml'
