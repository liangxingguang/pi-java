# 02 AI / Provider 层（pi-java-ai）

> 判定三档：**对齐** = pi 有 ∧ pi-java 有 ∧ 行为相同（读代码确认语义）；
> **缺失** = pi 有 ∧ pi-java 无（或只有严格更小的子集，且差异有行为后果）；
> **存疑** = pi-java 有但形状/行为未核，**或**差异是刻意设计/跨模块/待裁决。
> 所有证据为 `file:line`，两侧均已打开源码确认。
>
> **权重** = 用户可观察影响 × 频率（用户裁决口径，不含排期优先级）：
> **3** = 每轮对话都走/默认路径；**2** = 每次会话走/常用；**1** = 低频/边缘/纯内部；**0** = 非目标，**排除出分母**。
> 完成系数：对齐 1.0 ／ 存疑 0.5 ／ 缺失 0。

## 规模

| 模块 | pi 包 | pi LOC | java LOC | 比例 |
|---|---|---|---|---|
| 主源码 | `packages/ai/src` | 24383 | 11590 | 0.48 |
| 测试 | `packages/ai/test` | 37431（152 文件） | 8649（56 文件） | 0.23 |
| 合计 | — | 61814 | 20239 | 0.33 |

分目录（主源码）：

| 目录 | pi LOC | pi-java LOC | 说明 |
|---|---|---|---|
| `api/`（wire 适配器） | 11783 | `protocol/` 3888 | pi 13 个文件 + 共享层；pi-java 8 个适配器 |
| `providers/` | 2461 | `provider/` 1266 + `provider/builtin/` 460 | pi 39 provider + images + radius；pi-java 17 |
| `utils/` | 2469 | `utils/` 332 + `http/` 560 + `stream/` 675 + `thinking/` 160 | pi-java 拆得更细 |
| `auth/` | 3589 | `auth/` 1354 | pi 9 个 OAuth 流；pi-java 6 |
| 根目录 | 4036 | `catalog/` 1015 + `model/` 287 + `message/` 524 + `api/` 628 + `cli/` 357 | — |

模型目录数据（生成物，未计入上表）：pi 由 `scripts/generate-models.ts` 从 models.dev 生成，落到 `providers/data/*.json`（39 provider，本机未 checkout）；
pi-java 手写 35 条（`catalog/BuiltinCatalog.java` 13 条 + `provider/builtin/ModelData.java` 22 条）。

---

## Provider 适配器对齐

### A. Wire 适配器（pi 12 套）

| # | wire adapter | pi 有 | java 有 | 判定 | 权重 | 证据 |
|---|---|---|---|---|---|---|
| 1 | `anthropic-messages` | ✅ `api/anthropic-messages.ts`（1490） | ✅ `protocol/AnthropicMessagesApi.java` | 对齐（含子缺口见能力单元 3/8/26/38-42/45/47） | 3 | pi `api/anthropic-messages.ts:1`；java `protocol/AnthropicMessagesApi.java:55` |
| 2 | `openai-completions` | ✅ `api/openai-completions.ts`（1717） | ✅ `protocol/OpenAICompletionsApi.java` | 对齐（子缺口：3/43/44/49） | 3 | pi `api/openai-completions.ts:1`；java `protocol/OpenAICompletionsApi.java:85` |
| 3 | `openai-responses` | ✅ `api/openai-responses.ts`（390）+ `openai-responses-shared.ts`（793） | ✅ `protocol/OpenAIResponsesApi.java` + `ResponsesStreamProcessor.java` + `ResponsesMessageConverter.java` | 对齐（子缺口 8） | 3 | pi `api/openai-responses.ts:1`；java `protocol/OpenAIResponsesApi.java` |
| 4 | `azure-openai-responses` | ✅ `api/azure-openai-responses.ts`（338） | ✅ `protocol/AzureOpenAIResponsesApi.java` + `AzureOptions.java` | 对齐（共享 3 的转换器） | 1 | pi `api/azure-openai-responses.ts:1`；java `protocol/AzureOpenAIResponsesApi.java` |
| 5 | `google-generative-ai` | ✅ `api/google-generative-ai.ts`（526）+ `google-shared.ts`（452） | ✅ `protocol/GoogleGenerativeAiApi.java` | 对齐 | 3 | pi `api/google-generative-ai.ts:1`；java `protocol/GoogleGenerativeAiApi.java:38` |
| 6 | `mistral-conversations` | ✅ `api/mistral-conversations.ts`（941） | ✅ `protocol/MistralConversationsApi.java` | 对齐（子缺口 3） | 2 | pi `api/mistral-conversations.ts:1`；java `protocol/MistralConversationsApi.java:34` |
| 7 | `pi-messages` | ✅ `api/pi-messages.ts`（441） | ✅ `protocol/PiMessagesApi.java` | 存疑（形状自造，见能力单元 63） | 1 | pi `api/pi-messages.ts:1`；java `protocol/PiMessagesApi.java:225-226` |
| 8 | `openrouter-images`（images API，非 chat） | ✅ `api/openrouter-images.ts`（196） | ✅ `protocol/OpenRouterImagesApi.java` | 对齐 | 1 | pi `api/openrouter-images.ts:1`；java `protocol/OpenRouterImagesApi.java:32` |
| 9 | `bedrock-converse-stream` | ✅ `api/bedrock-converse-stream.ts`（1325） | ❌ | **缺失** | 1 | pi `api/bedrock-converse-stream.ts:1`；`grep -rn bedrock pi-java-ai/src/main` 零命中 |
| 10 | `google-vertex` | ✅ `api/google-vertex.ts`（598） | ❌ | **缺失** | 1 | pi `api/google-vertex.ts:1`；`pi-java-ai/src/main/.../protocol/` 无 Vertex |
| 11 | `openai-codex-responses` | ✅ `api/openai-codex-responses.ts`（1657） | ❌ | **缺失** | 1 | pi `api/openai-codex-responses.ts:1`；`Protocol.java:16-30` 无该常量 |
| 12 | `cloudflare-ai-binding`（transport shim，非 wire 格式） | ✅ `api/cloudflare-ai-binding.ts`（90） | ❌ | **缺失** | 1 | pi `api/cloudflare-ai-binding.ts:2`；无对应物 |

> `api/cloudflare.ts`（15 行）只是 base URL 常量，不计为适配器。
> pi-java 另有 2 个 pi 没有的 API 面：`api/EmbeddingApi.java`（源码注释自认「pi-java 独有」，`api/EmbeddingApi.java:3`）、`api/SimpleApi.java`（非流式便捷面，pi 对应 `streamSimple` 走 `api/simple-options.ts`，形状不同）。
> **权重口径**：3 = 主流 provider 的默认车道（anthropic / openai-completions / openai-responses / google）；2 = 主流但次常用（mistral）；1 = 边缘/企业/自造（azure / bedrock / vertex / codex / cloudflare-binding / pi-messages / images）。

### B. Provider 端点（pi 39 个 catalog provider + 2 动态）

pi 全部 39 个：`src/models.generated.ts:44-83`（类型声明）、`:85-124`（值）。pi-java 17 个：`provider/builtin/ProviderCatalog.java:27-44`。

| # | provider | pi 有 | java 有 | 判定 | 权重 | 证据 |
|---|---|---|---|---|---|---|
| 1 | `anthropic` | ✅ `providers/anthropic.ts:47-58` | ✅ `provider/AnthropicProvider.java:12-16` | 对齐（wire 同 `anthropic-messages`；auth 见能力单元 40） | 3 | 同上 |
| 2 | `openai` | ✅ `providers/openai.ts:8-15`（**`api: openAIResponsesApi()`**） | ✅ `provider/OpenAIProvider.java:24-26`（**默认 `OPENAI_COMPLETIONS`**，responses 需 `extra.protocol` 选择） | **存疑（默认 wire 不同）** | 3 | pi `providers/openai.ts:13`；java `provider/OpenAIProvider.java:25-26` |
| 3 | `google` | ✅ `providers/google.ts:10-13` | ✅ `provider/GoogleProvider.java:16-17` | 对齐（baseUrl 尾斜杠差异 `.../v1beta` vs `...`） | 3 | pi `providers/google.ts:10`；java `provider/GoogleProvider.java:16` |
| 4 | `deepseek` | ✅ `providers/deepseek.ts:10-13` | ✅ `provider/DeepSeekProvider.java:22-28` | 对齐 | 3 | pi `providers/deepseek.ts:10`；java `provider/DeepSeekProvider.java:22` |
| 5 | `mistral` | ✅ `providers/mistral.ts:10-13` | ✅ `provider/MistralProvider.java:16-17` | 对齐 | 3 | 同上 |
| 6 | `moonshotai` | ✅ `providers/moonshotai.ts:10-13` | ✅ `provider/builtin/MoonshotAiProvider.java:15-16` | 对齐 | 2 | 同上 |
| 7 | `moonshotai-cn` | ✅ `providers/moonshotai-cn.ts:10-13` | ✅ `provider/builtin/MoonshotAiCnProvider.java:15-16` | 对齐 | 2 | 同上 |
| 8 | `zai` | ✅ `providers/zai.ts:10-13` | ✅ `provider/builtin/ZaiProvider.java:15-16` | 对齐 | 2 | 同上 |
| 9 | `zai-coding-cn` | ✅ `providers/zai-coding-cn.ts:10-13` | ✅ `provider/builtin/ZaiCodingCnProvider.java:15-16` | 对齐 | 2 | 同上 |
| 10 | `minimax` | ✅ `providers/minimax.ts:10-13` | ✅ `provider/builtin/MiniMaxProvider.java:15-16` | 对齐 | 1 | 同上 |
| 11 | `minimax-cn` | ✅ `providers/minimax-cn.ts:10-13` | ✅ `provider/builtin/MiniMaxCnProvider.java:15-16` | 对齐 | 1 | 同上 |
| 12 | `ant-ling` | ✅ `providers/ant-ling.ts:10-13` | ✅ `provider/builtin/AntLingProvider.java:15-16` | 对齐 | 1 | 同上 |
| 13 | `qwen-token-plan-cn` | ✅ `providers/qwen-token-plan-cn.ts:10-13` | ✅ `provider/builtin/QwenTokenPlanCnProvider.java:13-19` | 对齐 | 1 | 同上 |
| 14 | `xiaomi` | ✅ `providers/xiaomi.ts:10-13` | ✅ `provider/builtin/XiaomiProvider.java:15-16` | 对齐 | 1 | 同上 |
| 15 | `xiaomi-token-plan-cn` | ✅ `providers/xiaomi-token-plan-cn.ts:10-13` | ✅ `provider/builtin/XiaomiTokenPlanCnProvider.java:15-16` | 对齐 | 1 | 同上 |
| 16 | `openrouter` | ✅ `providers/openrouter.ts:12-22` | ❌（只有 `OpenRouterImagesProvider`，chat 面缺失） | **缺失** | 3 | pi `providers/openrouter.ts:12`；java 只有 `provider/builtin/OpenRouterImagesProvider.java:27`（`Set.of()` 无 chat 协议） |
| 17 | `openrouter-images`（provider，非 catalog） | ✅ `providers/openrouter-images.ts` | ✅ `provider/builtin/OpenRouterImagesProvider.java` | 对齐 | 1 | pi `providers/all.ts:29`；java `provider/builtin/ProviderCatalog.java:42` |
| 18 | `ollama` | ❌ pi 无 | ✅ `provider/builtin/OllamaProvider.java` | **存疑（pi-java 扩展）** | 2 | pi `models.generated.ts:44-83` 无 ollama；java `provider/builtin/ProviderCatalog.java:43` |
| 19 | `xai` | ✅ `providers/xai.ts` | ❌ | **缺失** | 2 | pi `models.generated.ts:75`；`ProviderCatalog.all()` 无 |
| 20 | `radius`（动态） | ✅ `providers/radius.ts` + `radius-config.ts` | ❌ | **缺失** | **0（排除）** | pi `providers/all.ts:32`；`pi-java-ai` 零命中 |
| 21 | 其余 **22 个 provider 长尾**：`amazon-bedrock` `azure-openai-responses` `baseten` `cerebras` `cloudflare-ai-gateway` `cloudflare-workers-ai` `fireworks` `github-copilot` `google-vertex` `groq` `huggingface` `kimi-coding` `nvidia` `openai-codex` `opencode` `opencode-go` `qwen-token-plan` `qwen-token-plan-individual` `together` `vercel-ai-gateway` `xiaomi-token-plan-ams` `xiaomi-token-plan-sgp` | ✅ 各一个 `providers/*.ts` | ❌ | **缺失** | **0（排除）** | pi `models.generated.ts:44-83` 逐项；`ProviderCatalog.all()` 只列 17 个 |

> 计数：pi 39 catalog + `openrouter-images` + `radius` = 41；pi-java 17（含 1 个 pi 没有的 `ollama`）。
> 覆盖：pi-java 覆盖 pi 的 **15/41**，缺 **24**（含 openrouter chat 面），多 1（ollama）。
> 22 个缺失中 14 个只是 `openai-completions` relay（baseten/cerebras/fireworks/groq/huggingface/nvidia/together/vercel-ai-gateway/opencode/opencode-go/kimi-coding/cloudflare-*/qwen-token-plan-individual/xai），wire 层已被 #2 覆盖 ⇒ 缺的是**端点条目 + 环境变量 + 目录数据**，不是新适配器。
>
> **权重口径（用户裁决：provider 只实现主流的就行）**：主流子集 = **Anthropic / OpenAI / Google / xAI / DeepSeek / Mistral / OpenRouter（聚合）** ⇒ 这 7 家取 2–3。pi-java **已有的**国内厂商端点（moonshotai / zai / minimax / ant-ling / qwen-token-plan-cn / xiaomi 各系）**不因「国内」而排除**，按覆盖情况取 1–2。`ollama` 与 `openrouter-images` 各取 2 / 1。**这 7 家之外、且 pi-java 没有的 23 个端点（#20 `radius` + #21 的 22 个）权重 0，排除出分母。**

---

## 能力单元清单（除 provider 外）

### ContentBlock 变体（每车道收发）

| # | 能力单元 | 类别 | pi 侧证据 | pi-java 侧证据 | 判定 | 权重 | 台账条目 |
|---|---|---|---|---|---|---|---|
| 1 | `TextContent` 收发 | ContentBlock | `types.ts:351-355`；`anthropic-messages.ts:145,1282` | `message/ContentBlock.java:31`；`AnthropicMessagesApi.java:440-455` | 对齐 | 3 | B11（已修） |
| 2 | `ThinkingContent` 收发（含 signature） | ContentBlock | `types.ts:357-365`；`anthropic-messages.ts:1287-1321` | `ContentBlock.java:47-61`；`AnthropicMessagesApi.java:468-472,477-520` | 对齐 | 3 | B6/B7/B9/B13（已修） |
| 3 | `ImageContent` 收发 | ContentBlock | `types.ts:367-371`；`anthropic-messages.ts:121-160`（`convertContentBlocks`，**user 与 toolResult 两处**）、`:1250-1260`；`openai-completions.ts:1248-1250,1415-1416`；`mistral-conversations.ts:798` | `ContentBlock.java:73`；`AnthropicMessagesApi.java:465`（丢弃）、`:536-545`（`toTextBlocks` 只留 TextContent）；`OpenAICompletionsApi.java:417,427`（`extractText` 只留文本）；`MistralConversationsApi.java:290,330`（同） | **缺失** | 2 | B17（范围写错，见台账纠错） |
| 4 | `UrlImageContent` 收发 | ContentBlock | pi **无此变体** | `ContentBlock.java:84`；`GoogleGenerativeAiApi.java:348`、`ResponsesMessageConverter.java:160`、`PiMessagesApi.java:237` | 存疑（pi-java 扩展） | 1 | — |
| 5 | `DiffContent` | ContentBlock | pi **无此变体** | `ContentBlock.java:125`；`GoogleGenerativeAiApi.java:322` | 存疑（pi-java 扩展，UI 用） | 1 | — |
| 6 | `ToolUseContent` 收发 | ContentBlock | `types.ts:373-381`；`anthropic-messages.ts:1330-1360` | `ContentBlock.java:92`；`AnthropicMessagesApi.java:459-467` | 对齐 | 3 | — |
| 7 | `ToolUseContent.thoughtSignature` | ContentBlock | `types.ts:378`；`transform-messages.ts:131-134` | 无（`grep -rn thoughtSignature pi-java-ai/src/main` 零命中） | **缺失** | 1 | B14② |
| 8 | `ToolUseContent.namespace` | ContentBlock | `types.ts:380` | 无 | **缺失** | 1 | — |
| 9 | `ToolResultContent` 收发 | ContentBlock | `types.ts:452-468`；`anthropic-messages.ts:1183-1215` | `ContentBlock.java:110`；`AnthropicMessagesApi.java:378-392,525-534` | 对齐 | 3 | — |
| 10 | 孤儿 toolCall 合成 `toolResult` | ContentBlock | `transform-messages.ts:158-220`（「No result provided」） | 无 | **缺失** | 2 | B14④ |
| 11 | `TextContent.textSignature` | ContentBlock | `types.ts:355`；`openai-responses-shared.ts:55,228,701` | 无（`grep -rn textSignature pi-java-ai/src/main` 零命中） | **缺失** | 1 | B23 |
| 12 | 空 text 块丢弃 | ContentBlock | `anthropic-messages.ts:1282`（trim 判空） | `AnthropicMessagesApi.java:447-450` | 对齐 | 1 | B11（已修） |
| 13 | 跨模型 toolCall id 归一 | ContentBlock | `transform-messages.ts:136-142` | 无 | **缺失** | 1 | B14③ |
| 14 | 图片降级为占位文本（按 `model.input`） | ContentBlock | `transform-messages.ts:35-57` | 无（`Model.input` 本身也缺，见 24） | **缺失** | 1 | B14① |

### StreamEvent 类型

| # | 能力单元 | 类别 | pi 侧证据 | pi-java 侧证据 | 判定 | 权重 | 台账条目 |
|---|---|---|---|---|---|---|---|
| 15 | `start` | StreamEvent | `types.ts:547` | `stream/StreamEvent.java:85` | 对齐 | 3 | — |
| 16 | `text_start` / `text_delta` / `text_end` | StreamEvent | `types.ts:548-550` | `StreamEvent.java:95/102/109` | 对齐 | 3 | — |
| 17 | `thinking_start` / `thinking_delta` / `thinking_end` | StreamEvent | `types.ts:551-553` | `StreamEvent.java:119/127/134` | 对齐 | 3 | — |
| 18 | `toolcall_start` / `toolcall_delta` / `toolcall_end` | StreamEvent | `types.ts:554-556` | `StreamEvent.java:146/154/164` | 对齐 | 3 | — |
| 19 | `done` 载荷 | StreamEvent | `types.ts:557-561`（`reason` + **`message: AssistantMessage`**） | `StreamEvent.java:222`（`reason` + **`usage: UsageInfo`** + `partial`） | 存疑（载荷不同形） | 3 | — |
| 20 | `error` 载荷 | StreamEvent | `types.ts:562`（`reason` + **`error: AssistantMessage`**） | `StreamEvent.java:231`（`reason` + **`Throwable`** + `partial`） | 存疑（载荷不同形） | 2 | — |
| 21 | `UsageInfo` 事件 | StreamEvent | pi **无此事件**（usage 走 partial） | `StreamEvent.java:182` | 存疑（pi-java 扩展） | 2 | — |
| 22 | `partial.stopReason` 初值 `"pending"` | StreamEvent | `anthropic-messages.ts:526`、`openai-completions.ts:333`、`google-generative-ai.ts:75`、`mistral-conversations.ts:222`、`openai-responses.ts:139` | `AnthropicMessagesApi.java:55`、`GoogleGenerativeAiApi.java:72`、`MistralConversationsApi.java:34` | 对齐 | 1 | B26（已修） |
| 23 | signature 多发一条 `ThinkingDelta` | StreamEvent | pi 只改块、不 push | `stream/StreamPartialBuilder.java:217` | 存疑（待裁决，无剧本可证） | 1 | C8 |

### 模型目录 / 模型元数据字段

| # | 能力单元 | 类别 | pi 侧证据 | pi-java 侧证据 | 判定 | 权重 | 台账条目 |
|---|---|---|---|---|---|---|---|
| 24 | `Model.input: ("text"\|"image")[]` | 元数据 | `types.ts:857` | 无对应字段（`ModelCapability.IMAGE_INPUT` 是能力集，不是数组；`model/ModelCapability.java:20`） | **缺失** | 1 | — |
| 25 | `Model.cost: ModelCost`（含 `cacheRead`/`cacheWrite`/`tiers`） | 元数据 | `types.ts:827-842` | `model/PricingInfo.java:12` 只有 `inputPrice`/`outputPrice` | **缺失** | 2 | — |
| 26 | `Model.compat` | 元数据 | `types.ts:610-770`：`OpenAICompletionsCompat` **26** + `OpenAIResponsesCompat` **9** + `AnthropicMessagesCompat` **12** + `BedrockCompat` **1** = **48** 字段（逐接口 `awk` 计数） | `catalog/ModelCompat.java:63`（3 字段：`allowEmptySignature` / `requiresReasoningContentOnAssistantMessages` / `supportsFinishReason`） | **缺失**（3/48；缺的那批有行为后果：`supportsStore`/`maxTokensField`/`supportsDeveloperRole`/`requiresToolResultName`/`thinkingFormat`/`cacheControlFormat`/`sessionAffinityFormat`/`deferredToolsMode`） | 2 | B8（已修，仅 1 字段）、B21 |
| 27 | `Model.baseUrl` 在 `Model` 上 | 元数据 | `types.ts:850` | 不在 `ModelInfo`（`catalog/ModelInfo.java:30-41`），在 `ApiOptions`/`ProviderConfig` | 存疑（位置不同，`OpenAICompletionsApi.java:107` 注释已记录该取舍） | 1 | — |
| 28 | `Model.reasoning` | 元数据 | `types.ts:851` | `ModelCapability.THINKING`（`ModelCapability.java:26`） | 存疑（形状不同；且**不可达**） | 2 | B15 |
| 29 | `Model.thinkingLevelMap` | 元数据 | `types.ts:856` | `thinking/ThinkingLevelMap.java:14` | 存疑（存在但**生产不可构造**） | 2 | B15 |
| 30 | `ThinkingLevel` 词表含 `"max"` | 元数据 | `types.ts:83`（6 档：minimal/low/medium/high/xhigh/**max**） | `thinking/ThinkingLevel.java:28-35`（5 档，无 `max`）；`ResponsesOptions.java:69` 把 `"max"` 折进 `XHigh` | **缺失** | 1 | — |
| 31 | `ModelsStore`（read/write/delete + etag/lastModified/checkedAt） | 元数据 | `models-store.ts:17-23`、`InMemoryModelsStore:25` | `catalog/ModelsStore.java`、`ModelsStoreEntry.java`、`InMemoryModelsStore.java`、`FileModelsStore.java` | 对齐 | 2 | — |
| 32 | `ModelsStoreEntry.etag` / `lastModified` / `checkedAt` | 元数据 | `models-store.ts:5-15` | `catalog/ModelsStoreEntry.java`；`RemoteCatalog.java:30` 注释确认 ETag 原样存取 | 对齐 | 1 | — |
| 33 | `ImagesModel` / `ImagesModels` 注册表 | 元数据 | `images-models.ts:12-90,227`、`images-api-registry.ts:53`、`image-models.generated.ts`（789） | 无（`grep -rn ImagesModel pi-java-ai/src/main` 零命中） | **缺失** | 1 | — |
| 34 | 内置模型目录数据规模 | 元数据 | models.dev 生成，39 provider（`scripts/generate-models.ts`） | 35 条手写（`catalog/BuiltinCatalog.java` 13 + `provider/builtin/ModelData.java` 22） | **缺失**（规模差 1-2 个数量级） | 2 | — |
| 35 | `samplingParams` / `headers` | 元数据 | `types.ts:862-863` | `ModelInfo.java:38-39` | 对齐 | 1 | — |
| 36 | `contextWindow` / `maxTokens` | 元数据 | `types.ts:859-860` | `ModelInfo.java:35-36`（`maxInputTokens`/`maxOutputTokens`） | 对齐 | 3 | — |
| 37 | `Model.responseModel` / `responseId` / `providerThinkingLevel` / `diagnostics` / `endTurn` | 元数据 | `types.ts:430-448` | `message/AssistantMessage.java:55-63` 无这些组件（10 组件：id/content/usage/stopReason/api/provider/model/timestamp/errorMessage/rawStopReason） | **缺失** | 1 | — |
| 38 | `ToolResultMessage.usage` / `details` / `addedToolNames` | 元数据 | `types.ts:452-468` | `Message.java`（`rawStopReason` 已在，`addedToolNames`/`details` 无生产者） | 存疑 | 1 | B4→C11（结案为不做） |

### 鉴权 / base-url / 环境变量

| # | 能力单元 | 类别 | pi 侧证据 | pi-java 侧证据 | 判定 | 权重 | 台账条目 |
|---|---|---|---|---|---|---|---|
| 39 | env API key 映射（按已注册 provider） | 鉴权 | `env-api-keys.ts:78-113`（38 项） | `auth/EnvApiKeyResolver.java:20`（从 `ProviderCatalog` 派生，17 项全覆盖） | 对齐（按已注册 provider 计） | 3 | — |
| 40 | `ANTHROPIC_AUTH_TOKEN`（`Authorization: Bearer`） | 鉴权 | `env-api-keys.ts:29,73-76`；`providers/anthropic.ts:24-31` | 无（`grep -rn AUTH_TOKEN pi-java` 零命中；`AnthropicMessagesApi.java:81` 只走 SDK `.apiKey()`） | **缺失** | 2 | — |
| 41 | `ANTHROPIC_OAUTH_TOKEN` | 鉴权 | `env-api-keys.ts:30`；`providers/anthropic.ts:34-37` | 无 | **缺失** | 1 | — |
| 42 | provider 凭证优先级链（stored → authToken → oauthToken → apiKey） | 鉴权 | `providers/anthropic.ts:18-42` | 无（`AuthProfileManager` 是另一套：profile 名 + `_<PROFILE>` 后缀） | **缺失** | 2 | — |
| 43 | OAuth 流：`openrouter` / `anthropic` / `xai` / `kimi` / `github-copilot` / `openai-codex` | 鉴权 | `auth/oauth/*.ts`（6 个文件共 2013 行） | `auth/OAuthProviders.java:18-56` | 对齐 | 2 | — |
| 44 | OAuth 流：`radius` | 鉴权 | `auth/oauth/radius.ts`（403） | 无 | **缺失** | **0（排除）** | — |
| 45 | OAuth `device-code` 通用流 + PKCE | 鉴权 | `auth/oauth/device-code.ts:98`、`pkce.ts:34` | `auth/DeviceCodeFlow.java`、`auth/DeviceCodeConfig.java` | 对齐 | 2 | — |
| 46 | 多 profile 凭证（`~/.pi-java/auth-profiles.json`） | 鉴权 | pi 无此构造 | `auth/AuthProfileManager.java:19-24`、`auth/FileCredentialStore.java` | 存疑（pi-java 扩展，P6-18） | 1 | — |
| 47 | 自定义 provider（`models.json`） | 鉴权 | pi 走 `models-store` + `createProvider`（`models.ts:775`） | `provider/ModelsJsonProvider.java`、`ModelsJsonConfig.java`、`ModelsJsonSchema.java` | 存疑（形状不同，非逐项对照） | 2 | — |
| 48 | `google-vertex` ADC 凭证探测 | 鉴权 | `env-api-keys.ts:33-75` | 无（provider 本身缺失） | **缺失** | **0（排除）** | — |
| 49 | `amazon-bedrock` 多凭证源（AWS_PROFILE / IAM / bearer / ECS / IRSA） | 鉴权 | `env-api-keys.ts:158-180` | 无（provider 本身缺失） | **缺失** | **0（排除）** | — |
| 50 | `env-api-keys.ts` 的 `ProviderEnv` 可注入环境 | 鉴权 | `types.ts:113`、`utils/provider-env.ts`（52） | 无（`EnvApiKeyResolver.java:29` 直读 `System.getenv`） | 存疑 | 1 | — |

### token 计量 / usage 字段

| # | 能力单元 | 类别 | pi 侧证据 | pi-java 侧证据 | 判定 | 权重 | 台账条目 |
|---|---|---|---|---|---|---|---|
| 51 | `usage.input` / `usage.output` | usage | `anthropic-messages.ts:605-606` | 五条车道全走 `builder.emitUsage(a,b)`：`AnthropicMessagesApi.java:286-288`、`OpenAICompletionsApi.java:182,254`、`GoogleGenerativeAiApi.java:141`、`MistralConversationsApi.java:251`、`ResponsesStreamProcessor.java:247`、`PiMessagesApi.java:125-126` | 对齐 | 3 | — |
| 52 | `usage.cacheRead` | usage | `anthropic-messages.ts:607,761`；`openai-completions.ts:1519-1520`；`google-generative-ai.ts:230` | **零生产者**：`emitUsage` 的 3 参构造器把 `usage` 置 null（`StreamPartialBuilder.java:321`）⇒ `Message.usageOf` 兜 `Usage.of(a,b)` ⇒ `cacheRead=0`（`Message.java:155-158`） | **缺失** | 3 | — |
| 53 | `usage.cacheWrite` / `cacheWrite1h` | usage | `anthropic-messages.ts:608-609,764` | 同上，恒 0 | **缺失** | 3 | — |
| 54 | `usage.reasoning` | usage | `anthropic-messages.ts:766-770`（`output_tokens_details.thinking_tokens`）；`openai-completions.ts:1514,1536` | 同上，恒 null | **缺失** | 2 | — |
| 55 | `usage.totalTokens` 由分量合成 | usage | `anthropic-messages.ts:611-612`（`input+output+cacheRead+cacheWrite`） | `Usage.of` 用 `input+output`（`Usage.java:55`） | 存疑（今天数值相同，因 52/53 恒 0；52/53 修好后即分歧） | 2 | — |
| 56 | `usage.cost` 计算（`calculateCost`） | usage | `models.ts:891-911`（含 1h 缓存 2× 定价、`tiers` 阶梯） | **全仓无对应物**：`Usage.Cost.zero()` 恒零（`Usage.java:44-46`）；`PricingInfo` 只有 input/output（`PricingInfo.java:12`） | **缺失** | 3 | 台账未登记 |
| 57 | `choice.usage` 回退（Moonshot 型 relay） | usage | `openai-completions.ts:565-568` | 无：`OpenAICompletionsApi.java:252-255` 只看 `chunk.usage()` | **缺失** | 1 | B24（仍成立） |
| 58 | `parseChunkUsage` 归一（`prompt_tokens_details.cached_tokens` / `prompt_cache_hit_tokens` / `cached_tokens` 三路 + 减法语义） | usage | `openai-completions.ts:1507-1540` | 无：直取 `u.promptTokens()`/`u.completionTokens()` | **缺失** | 2 | — |
| 59 | `message_start` 首帧 usage 与 `message_delta` 逐字段覆盖 | usage | `anthropic-messages.ts:602-612`（首帧）+ `:757-775`（逐字段 `!= null` 才覆盖） | `AnthropicMessagesApi.java:285-288` 只取 delta，且用 `orElse(0L)` **无条件覆盖** | **缺失**（会抹掉 message_start 的 input_tokens） | 2 | — |

### 横切能力

| # | 能力单元 | 类别 | pi 侧证据 | pi-java 侧证据 | 判定 | 权重 | 台账条目 |
|---|---|---|---|---|---|---|---|
| 60 | `sanitizeSurrogates`（出站孤对代理清理） | 横切 | `utils/sanitize-unicode.ts:21-25`；全仓 57 处调用 | 零命中（`grep -rni surrogate pi-java-ai/src/main`） | **缺失** | 1 | B16（仍成立） |
| 61 | `transformMessages` 跨模型重放闸 | 横切 | `transform-messages.ts:64-222`（5 条变换） | `api/TransformMessages.java:43-137`（**1/5**：thinking 五分支）；已挂 5 车道：`AnthropicMessagesApi.java:354`、`OpenAICompletionsApi.java:413`、`GoogleGenerativeAiApi.java:110`、`MistralConversationsApi.java:292`、`ResponsesMessageConverter.java:114` | **缺失**（1/5；其余 4 条 = 能力单元 7/10/13/14） | 3 | B10（已修）、B14（仍成立） |
| 62 | Anthropic `cache_control` 标记（prompt caching） | 横切 | `anthropic-messages.ts:67-75,1027,1071-1087,1109,1374-1391,1459` | 零命中（`grep -rn cache_control pi-java-ai/src/main`） | **缺失** | 3 | — |
| 63 | `PiMessagesApi` 的 thinking 线格（signature/redacted） | 横切 | pi 的 6 个请求构建器无对应物 | `PiMessagesApi.java:225-226` 一律 `{type:"thinking","thinking":text}` | 存疑（pi 无此车道，按 pi 的闸改＝发明行为） | 1 | B18（仍成立） |
| 64 | constrained sampling / grammar 工具 | 横切 | `api/constrained-sampling.ts`（277）；10 条车道都 import | 零命中 | **缺失** | 1 | — |
| 65 | 请求侧 `compat.thinkingFormat`（10 种 thinking 开关形状） | 横切 | `openai-completions.ts:866,879,887,892,897,914,924,934,939,948` | 无（只有 `AnthropicMessagesApi.java:424-434` 的 `thinking.budgetTokens`） | **缺失** | 2 | B21（仍成立） |
| 66 | stop reason 映射（5 车道） | 横切 | `anthropic-messages.ts:1464-1493`；`openai-completions.ts:571-577`；`google-shared.ts:379-411`；`mistral-conversations.ts:926-941` | `AnthropicMessagesApi.java:300-340`（`max_tokens`→`length`）、`OpenAICompletionsApi.java:269-289`、`GoogleGenerativeAiApi.java:149-156`、`MistralConversationsApi.java:151-154`、`ResponsesStreamProcessor.java:190-197` | 对齐 | 3 | B20（已修） |
| 67 | `supportsFinishReason` 门（缺 `finish_reason` 即抛） | 横切 | `openai-completions.ts:571-577` | `OpenAICompletionsApi.java:269-289` | 对齐 | 2 | B20①（已修） |
| 68 | `assistant-message-frame`（可重放进度帧） | 横切 | `utils/assistant-message-frame.ts`（490） | pi-java 侧对应物在 `pi-java-web`（`WebWireJson`）/`pi-java-protocol`（`TranscriptProgress`），**不在 ai 模块** | 存疑（跨模块） | 2 | B28/B29（RPC 面） |
| 69 | `simple-options`（`clampMaxTokensToContext` / `thinkingBudgetForLevel` / `adjustMaxTokensForThinking` / `DEFAULT_THINKING_BUDGETS`） | 横切 | `api/simple-options.ts:1-95` | 零命中；`agent-core/StreamSimple` 存在但**生产不可达**（B15 已证 3 个调用点全在测试） | **缺失** | 2 | B15（半已修） |
| 70 | `ContextOverflow` 溢出判定 | 横切 | `utils/overflow.ts`（180） | `utils/ContextOverflow.java`（逐条移植，含 3 种情形） | 对齐 | 2 | — |
| 71 | `RetryableError` 分类（白名单/黑名单模式） | 横切 | `utils/retry.ts:1-107` | `utils/RetryableError.java:41-112`（同列表） | 对齐 | 2 | — |
| 72 | `RetryBackoff`（`retryDelayMs`） | 横切 | `utils/retry.ts:111-115` | `utils/RetryBackoff.java:33-37` | 对齐 | 2 | — |
| 73 | `provider-retry`（`x-should-retry` 头 + `validateServerRetryDelayMs`） | 横切 | `utils/provider-retry.ts:1-125` | `http/RetryPolicy.java:14-70`（预设 anthropic/openai/google）+ `PiHttpClient` | 存疑（形状不同：pi 读 `x-should-retry` 头，pi-java 用状态码集合） | 2 | A7（未做） |
| 74 | `transport: "sse"\|"websocket"\|"websocket-cached"\|"auto"` | 横切 | `types.ts:110`；`openai-codex-responses.ts:286-293,1488` | 无（承载它的 codex 车道本身缺失） | **缺失** | 1 | — |
| 75 | `DeferredHandle` / deferred responses（`fetchDeferred`/`cancelDeferred`） | 横切 | `types.ts:410`；`api/lazy.ts:71-89` | `message/DeferredHandle.java`（**无生产者**，注释自认「pi 同状」） | 存疑（两侧同状） | 1 | — |
| 76 | `lazyApi` / `lazyStream` 延迟加载注册表 | 横切 | `api/lazy.ts:42-98` | `provider/ProviderFactory.java`、`ProviderRegistry.java`、`Protocol.java` | 存疑（形状不同） | 1 | C9 |
| 77 | `images` 入口（`generateImages` + registry） | 横切 | `images.ts:21`、`images-api-registry.ts`、`providers/images/register-builtins.ts:50` | `protocol/OpenRouterImagesApi.java:32` + `api/ImageApi.java` + `ImageRequest/ImageResult/ImageStopReason` | 对齐（入口无 registry 层，单 provider） | 1 | — |
| 78 | `session-resources` 清理注册表 | 横切 | `session-resources.ts:1-24` | 零命中（pi-java 侧生命周期在 agent-core） | **缺失** | 1 | — |
| 79 | `validateToolArguments` / `validateToolCall`（typebox schema 校验） | 横切 | `utils/validation.ts:302-350` | pi-java 侧在 `agent-core/tool/ToolArgumentsValidator.java:38` | 对齐（跨模块，本模块不重复实现） | 3 | — |
| 80 | `FauxProvider`（测试桩） | 横切 | `providers/faux.ts` | `provider/FauxProvider.java`；`FauxChatApi extends AbstractChatApi`（`:164`） | 对齐 | 1 | A16（已修） |

---

## 台账纠错

| 条目 | 台账说什么 | 实际 | 证据 |
|---|---|---|---|
| **B17 范围** | 「**不是「图片进不了 Message」**：Google / OpenAI-Responses / PiMessages 三条车道**都**映射了图片 ⇒ 是 **Anthropic 车道独缺**」 | **错**。丢弃图片的是**四条**车道中的**三条**：`anthropic-messages` **＋ `openai-completions` ＋ `mistral-conversations`**。`OpenAICompletionsApi.buildParams` 用 `extractText()` 把 user 与 toolResult 的 content 拍平成纯文本（`:417`、`:427`），`MistralConversationsApi` 同样（`:290`、`:298/:302/:309` 走 `extractText`）。pi 侧三条都映射（`openai-completions.ts:1248-1250`、`:1415-1416`；`mistral-conversations.ts:798`） | pi-java `OpenAICompletionsApi.java:417,427,330`；`MistralConversationsApi.java:290,330`；pi `openai-completions.ts:1248-1250,1415-1416`；`mistral-conversations.ts:798` |
| **B17 具体行为** | 「`AnthropicMessagesApi.toBlockParams` 只处理 Text/Thinking/ToolUse ⇒ `ImageContent`/`UrlImageContent`/`DiffContent` **无声消失**」 | **成立，且比台账更严重**：toolResult 路径也丢。`toToolResultBlock` → `toTextBlocks`（`:536-545`）只收 `TextContent` ⇒ **ReadTool 读图（`ReadTool.java:86` 返回 `ContentBlock.ImageContent`）在 Anthropic 车道上模型完全看不到图**，只看到同批的文本行。pi 侧 `convertContentBlocks`（`anthropic-messages.ts:121-160`）明确处理 toolResult 的图片（`:1206` 调用点） | pi-java `AnthropicMessagesApi.java:465,536-545`；`pi-java-agent-core/.../ReadTool.java:86`；pi `anthropic-messages.ts:121-160,1206` |
| **B16 调用计数** | 「全仓 **54 处**调用」 | 实测 **57 处**（`grep -rn sanitizeSurrogates packages/ai/src \| wc -l`）。结论（pi-java 零处）不变 | `packages/ai/src/utils/sanitize-unicode.ts:21` 定义 + 56 调用点 |
| **B22 状态** | 「**仍必修**…（今天 CLI 不可达）」 | **已修**。`ResponsesMessageConverter.java:212` 已设 `.id("msg_pi_" + msgIndex)`；但**只实现了合成 id 分支**，pi 的 `parseTextSignature(...)?.id` 优先分支随 B23 一起缺（台账 B23 已如实记录） | pi-java `ResponsesMessageConverter.java:210-212`；pi `openai-responses-shared.ts:237-242` |
| **B24 状态** | 「本包不改」 | **仍成立**，且台账「计费恒为 0」的表述**低估**：`choice.usage` 只是**一路**回退；即便走标准 `chunk.usage`，pi-java 也**不读 `prompt_tokens_details.cached_tokens`**（台账未记），且 `usage.cost` **永不计算**（见下条） | pi-java `OpenAICompletionsApi.java:252-255`；pi `openai-completions.ts:565-568,1507-1540` |
| **（新）`calculateCost` 无登记** | 台账无此条目 | **pi 有 `calculateCost`（`models.ts:891-911`，含 1h 缓存 2× 输入价、`tiers` 阶梯），pi-java 全仓无对应物**。后果：`Usage.Cost.zero()` 恒零（`Usage.java:44-46`）⇒ `SessionState.java:332` 与 `SessionRunner.java:78-79` 累加的 `cost().total()` **恒为 0**，`~/.pi-java` 用量统计与 TUI 状态栏成本永远是 0。`PricingInfo`（只有 input/output）也不足以支撑 | pi-java `Usage.java:44-46`、`PricingInfo.java:12`、`SessionState.java:332`、`SessionRunner.java:78-79`；pi `models.ts:891-911` |
| **（新）cache/reasoning token 无生产者 无登记** | 台账无此条目 | **`Usage` 的 `cacheRead`/`cacheWrite`/`cacheWrite1h`/`reasoning` 四个分量在 pi-java 全仓零生产者**（五条车道的 `emitUsage` 全走 3 参构造器 ⇒ `usage == null` ⇒ `usageOf` 兜零）。但**下游已在读**：`ContextOverflow.java:110,120` 与 `ContextUsageEstimator.java:78` 都算 `input + cacheRead (+ cacheWrite)` ⇒ 开了 prompt caching 的请求**上下文占用被低估**（Anthropic 的 cache_read 通常远大于 input）⇒ 压缩时机会晚于 pi。`EntryJsonCodec.java:118-126` 也在读写这四个字段（落盘面已备好，只是没人填） | pi-java `StreamPartialBuilder.java:317-324`、`Message.java:150-159`、`ContextOverflow.java:110,120`、`ContextUsageEstimator.java:78`；pi `anthropic-messages.ts:602-612,757-775` |
| **（新）`openai` provider 默认 wire 不同 无登记** | 台账无此条目 | pi 的 `openai` provider 绑 **`openai-responses`**（`providers/openai.ts:13`），pi-java 绑 **`openai-completions`**（`OpenAIProvider.java:25`，responses 需显式 `extra.protocol`）⇒ **同一模型两侧发不同 wire 格式**，请求/响应/错误形状全线不同 | pi `providers/openai.ts:8-15`；pi-java `provider/OpenAIProvider.java:24-26` |
| **（新）`ANTHROPIC_AUTH_TOKEN` 无登记** | 台账无此条目 | pi 的 anthropic provider 有 4 级凭证链（stored → `ANTHROPIC_AUTH_TOKEN`（Bearer）→ `ANTHROPIC_OAUTH_TOKEN` → `ANTHROPIC_API_KEY`，`providers/anthropic.ts:18-42`）；pi-java 只有 `ANTHROPIC_API_KEY`（`AnthropicProvider.java:14` + SDK `.apiKey()`）⇒ 用 auth-token 形态的 Anthropic 兼容网关接不上 | pi `providers/anthropic.ts:18-42`、`env-api-keys.ts:29-31,73-76`；pi-java `provider/AnthropicProvider.java:12-16`、`protocol/AnthropicMessagesApi.java:79-81` |
| **A16 状态** | 「**登记，单独一包**」 | **已修**：`FauxChatApi extends AbstractChatApi`（`FauxProvider.java:164`），走同一条身份挂载缝；新夹具 `FauxProviderIdentityTest`（台账 §10 已记） | pi-java `provider/FauxProvider.java:164,175` |
| **A13 状态** | 「**待裁**」 | **已修**（台账同段已补记 ✅） | 台账 `docs/32` A13 行 ✅ 段 |

---

## 汇总

按上表 80 个能力单元 + 12 个 wire 适配器 + 41 个 provider 端点计（共 **133** 个判定单位）：

| 档 | 数 | 占比 |
|---|---|---|
| 对齐 | **47** | 35.3% |
| 缺失 | **63** | 47.4% |
| 存疑 | **23** | 17.3% |
| **合计** | **133** | 100% |

**完成度 = 47/133 = 35.3%**（严格档：只算「两侧都有且行为相同」）

分域完成度（对齐 / 缺失 / 存疑 / 合计）：

| 域 | 对齐 | 缺失 | 存疑 | 合计 | 完成度 |
|---|---|---|---|---|---|
| Wire 适配器 | 7 | 4 | 1 | 12 | 58.3% |
| Provider 端点 | 15 | 24 | 2 | 41 | 36.6% |
| ContentBlock 变体 | 5 | 7 | 2 | 14 | 35.7% |
| StreamEvent 类型 | 5 | 0 | 4 | 9 | 55.6% |
| 模型目录 / 元数据 | 4 | 7 | 4 | 15 | 26.7% |
| 鉴权 / env | 3 | 6 | 3 | 12 | 25.0% |
| token 计量 / usage | 1 | 7 | 1 | 9 | 11.1% |
| 横切 | 7 | 8 | 6 | 21 | 33.3% |
| **合计** | **47** | **63** | **23** | **133** | **35.3%** |

**最低分域 = token 计量 / usage（11.1%）**：整层「输入/输出两个数」之外全缺（cache/reasoning/cost/total 四类分量无生产者，回退路径无，归一函数无）。
**次低 = 鉴权 / env（25.0%）** 与 **模型目录 / 元数据（26.7%）**。

---

## 加权汇总

**权重口径**（用户裁决）：3 = 每轮对话都走/默认路径；2 = 每次会话走/常用；1 = 低频/边缘/纯内部；**0 = 非目标，排除出分母**。
完成系数：对齐 **1.0** ／ 存疑 **0.5** ／ 缺失 **0**。

**被排除的单位（权重 0，共 26 个，不进分母）**：
- provider 端点 **23 个**：`radius` ＋ 22 个长尾（`amazon-bedrock` `azure-openai-responses` `baseten` `cerebras` `cloudflare-ai-gateway` `cloudflare-workers-ai` `fireworks` `github-copilot` `google-vertex` `groq` `huggingface` `kimi-coding` `nvidia` `openai-codex` `opencode` `opencode-go` `qwen-token-plan` `qwen-token-plan-individual` `together` `vercel-ai-gateway` `xiaomi-token-plan-ams` `xiaomi-token-plan-sgp`）—— 均不在主流 7 家（Anthropic / OpenAI / Google / xAI / DeepSeek / Mistral / OpenRouter）之内，且 pi-java 没有。
- 鉴权 **3 条**：`OAuth radius`（44）／`google-vertex ADC`（48）／`amazon-bedrock 多凭证源`（49）—— 依附于被排除的 provider。

**分母**：134 个单位（80 能力单元 + 12 wire 适配器 + 42 provider 行）− 26 个权重 0 = **108 个单位**。

| 域 | Σ权重 | Σ(w×系数) | 加权完成度 | 未加权完成度 |
|---|---:|---:|---:|---:|
| Wire 适配器（12 单位） | 21 | 16.5 | **78.6%** | 58.3%（7/12） |
| Provider 端点（19 单位，已剔 23） | 37 | 29.5 | **79.7%** | 79.0%（15/19） |
| ContentBlock 变体（14） | 24 | 14.0 | **58.3%** | 35.7%（5/14） |
| StreamEvent 类型（9） | 21 | 17.0 | **81.0%** | 55.6%（5/9） |
| 模型目录 / 元数据（15） | 23 | 10.0 | **43.5%** | 26.7%（4/15） |
| 鉴权 / env（9 单位，已剔 3） | 16 | 9.0 | **56.3%** | 33.3%（3/9） |
| token 计量 / usage（9） | 21 | 4.0 | **19.0%** | 11.1%（1/9） |
| 横切（21） | 37 | 19.5 | **52.7%** | 33.3%（7/21） |
| **模块合计（108 单位）** | **200** | **119.5** | **59.75%** | **43.5%（47/108）** |

**模块合计**：Σ权重 200 ／ Σ(w×c) 119.5 ／ **加权完成度 = 119.5/200 = 59.75%**（未加权 43.5%；若把 26 个非目标单位也计入分母则未加权 47/134 = 35.1%，文件前文「汇总」节的 47/133 = 35.3% 用的是 41 行 provider 旧口径）

### 权重 3 的单元（29 个，Σ权重 87 ／ Σ(w×c) 66 ／ 完成度 75.9%）

| 域 | 单元 | 判定 |
|---|---|---|
| Wire | `anthropic-messages`（1） | 对齐 |
| Wire | `openai-completions`（2） | 对齐 |
| Wire | `openai-responses`（3） | 对齐 |
| Wire | `google-generative-ai`（5） | 对齐 |
| Provider | `anthropic` | 对齐 |
| Provider | `openai` | **存疑**（默认 wire 不同） |
| Provider | `google` | 对齐 |
| Provider | `deepseek` | 对齐 |
| Provider | `mistral` | 对齐 |
| Provider | `openrouter` | **缺失** |
| ContentBlock | `TextContent` 收发（1） | 对齐 |
| ContentBlock | `ThinkingContent` 收发（2） | 对齐 |
| ContentBlock | `ToolUseContent` 收发（6） | 对齐 |
| ContentBlock | `ToolResultContent` 收发（9） | 对齐 |
| StreamEvent | `start`（15） | 对齐 |
| StreamEvent | `text_start/delta/end`（16） | 对齐 |
| StreamEvent | `thinking_start/delta/end`（17） | 对齐 |
| StreamEvent | `toolcall_start/delta/end`（18） | 对齐 |
| StreamEvent | `done` 载荷（19） | **存疑** |
| 模型目录 | `contextWindow`/`maxTokens`（36） | 对齐 |
| 鉴权 | env API key 映射（39） | 对齐 |
| usage | `usage.input`/`output`（51） | 对齐 |
| usage | `usage.cacheRead`（52） | **缺失** |
| usage | `usage.cacheWrite`/`cacheWrite1h`（53） | **缺失** |
| usage | `usage.cost` 计算（56） | **缺失** |
| 横切 | `transformMessages` 跨模型重放闸（61） | **缺失**（1/5） |
| 横切 | Anthropic `cache_control` 标记（62） | **缺失** |
| 横切 | stop reason 映射（66） | 对齐 |
| 横切 | `validateToolArguments`（79） | 对齐 |

### 四条 P0 的加权边际

| P0 项 | 权重 | 修好后的 Σ(w×c) | 模块加权完成度 | 边际 |
|---|---:|---:|---:|---:|
| `usage.cacheRead` + `cacheWrite` + `reasoning`（52/53/54） | 3+3+2 | 127.5 | 63.75% | **+4.00pp** |
| `usage.cost` 计算（56） | 3 | 122.5 | 61.25% | **+1.50pp** |
| 图片丢弃（3，三条车道） | 2 | 121.5 | 60.75% | **+1.00pp** |
| `openai` 默认 wire（provider #2，存疑→对齐） | 3 | 121.0 | 60.50% | **+0.75pp** |
| **四条全修** | — | **134.0** | **67.00%** | **+7.25pp** |

## 存疑清单（全部 23 条）

**Provider / 适配器层（2）**
1. `openai` provider 默认 wire：pi=`openai-responses`（`providers/openai.ts:13`）/ pi-java=`openai-completions`（`OpenAIProvider.java:25`）—— 行为差异已定性，**未做逐帧对照**。
2. `ollama` provider：pi-java 有（`OllamaProvider.java`），pi 无 —— 扩展性质，**未核其 baseUrl/auth 与 pi 生态的兼容面**。
3. `pi-messages` wire（`PiMessagesApi.java`）：pi 有该 api 名（`types.ts:27`）但 pi-java 的**线格形状是自造的**（`PiMessagesApi.java:225-226`）—— **未与 pi 的 `api/pi-messages.ts` 逐帧对照**。

**ContentBlock（2）**
4. `UrlImageContent`（`ContentBlock.java:84`）：pi 无此变体 —— pi-java 扩展，**未核 pi 侧 URL 图片的真实表达**。
5. `DiffContent`（`ContentBlock.java:125`）：pi 无此变体 —— pi-java 扩展（UI 用），**未核是否应改挂到 details 通道**。

**StreamEvent（4）**
6. `done` 载荷（`StreamEvent.java:222` vs `types.ts:557-561`）：pi 带 `message`，pi-java 带 `usage`+`partial` —— **未核消费者是否需要完整 message**。
7. `error` 载荷（`StreamEvent.java:231` vs `types.ts:562`）：pi 带 `AssistantMessage`，pi-java 带 `Throwable` —— **未核错误路的形状对齐面**。
8. `UsageInfo` 事件（`StreamEvent.java:182`）：pi 无此事件 —— **未核是否应折进 partial**。
9. signature 多发 `ThinkingDelta`（`StreamPartialBuilder.java:217`）—— 台账 C8，**待裁决**。

**模型目录 / 元数据（4）**
10. `Model.baseUrl` 位置（`ModelInfo.java` 无，在 `ApiOptions`）—— 台账 `OpenAICompletionsApi.java:107` 已记取舍，**未核 detectCompat 的全部读点是否都拿得到**。
11. `Model.reasoning` → `ModelCapability.THINKING`（`ModelCapability.java:26`）—— **不可达**，台账 B15。
12. `Model.thinkingLevelMap`（`ThinkingLevelMap.java:14`）—— 存在但**生产不可构造**，台账 B15。
13. `ToolResultMessage.addedToolNames` / `details`（`Message.java` 无生产者）—— 台账 B4→C11 已结案为不做。

**鉴权 / env（3）**
14. 多 profile 凭证（`AuthProfileManager.java:19-24`）—— pi-java 扩展，**未核与 pi credential-store 的兼容面**。
15. `models.json` 自定义 provider（`ModelsJsonProvider.java`）—— 与 pi 的 `models-store` + `createProvider` **形状不同，未逐项对照**。
16. `env-api-keys.ts` 的 `ProviderEnv` 可注入环境（pi `types.ts:113`、`utils/provider-env.ts:52`）—— pi-java 直读 `System.getenv`（`EnvApiKeyResolver.java:29`），**未核是否需要**。

**usage（1）**
17. `usage.totalTokens` 合成口径（`Usage.java:55`）—— 今天与 pi 数值相同（因 cache 恒 0），**52/53 修好后即分歧**。

**横切（6）**
18. `PiMessagesApi` thinking 线格（`PiMessagesApi.java:225-226`）—— 台账 B18，pi 无此车道，**待独立设计**。
19. `assistant-message-frame`（pi `utils/assistant-message-frame.ts:1-490`）—— 对应物在 `pi-java-web`/`pi-java-protocol`，**跨模块未对照**。
20. `provider-retry` 形状（pi 读 `x-should-retry` 头 vs pi-java 状态码集合）—— 台账 A7，**未做映射对照**。
21. `DeferredHandle`（`DeferredHandle.java`）—— 两侧同状无生产者，**未核是否应删**。
22. `lazyApi` / `lazyStream`（`api/lazy.ts:42-98`）—— pi-java 用 `ProviderFactory`/`Protocol`，**形状不同未对照**；台账 C9（per-model `api` 表达不出）同族。
23. `validateToolArguments`（pi `utils/validation.ts:302-350`）—— pi-java 在 `agent-core/tool/ToolArgumentsValidator.java:38`，**跨模块未对照**。

---

## 缺失清单（全部 63 条）

**Wire 适配器（4）**
1. `bedrock-converse-stream`（pi `api/bedrock-converse-stream.ts:1`，1325 行）
2. `google-vertex`（pi `api/google-vertex.ts:1`，598 行）
3. `openai-codex-responses`（pi `api/openai-codex-responses.ts:1`，1657 行）
4. `cloudflare-ai-binding`（pi `api/cloudflare-ai-binding.ts:2`，transport shim）

**Provider 端点（25）**
5. `openrouter` chat 面（pi `providers/openrouter.ts:12`；pi-java 只有 `OpenRouterImagesProvider.java:27` 的 images 面）—— **主流 7 家之内**
6. `xai`（pi `providers/xai.ts`）—— **主流 7 家之内**
7. `radius`（pi `providers/radius.ts` + `radius-config.ts`，403 行 OAuth）—— 权重 0
8-29. 22 个长尾（**权重 0**）：`amazon-bedrock` / `azure-openai-responses` / `baseten` / `cerebras` / `cloudflare-ai-gateway` / `cloudflare-workers-ai` / `fireworks` / `github-copilot` / `google-vertex` / `groq` / `huggingface` / `kimi-coding` / `nvidia` / `openai-codex` / `opencode` / `opencode-go` / `qwen-token-plan` / `qwen-token-plan-individual` / `together` / `vercel-ai-gateway` / `xiaomi-token-plan-ams` / `xiaomi-token-plan-sgp`
   （`azure-openai-responses` 的**适配器在、provider 创建不在**：`ModelsJsonProvider.java:61-69` 只认两条车道）
   ⚠️ 其中只有 #5 `openrouter` 与 #6 `xai` 计入加权分母（各权重 3 / 2）；其余 23 个权重 0。

**ContentBlock（7）**
29. `ImageContent` 在 anthropic / openai-completions / mistral 三条车道的收发（含 toolResult 路径）
30. `ToolUseContent.thoughtSignature`
31. `ToolUseContent.namespace`
32. 孤儿 toolCall 合成 `toolResult`（`transform-messages.ts:158-220`）
33. `TextContent.textSignature`
34. 跨模型 toolCall id 归一（`transform-messages.ts:136-142`）
35. 图片降级为占位文本（`transform-messages.ts:35-57`）

**模型目录 / 元数据（7）**
36. `Model.input: ("text"|"image")[]`
37. `Model.cost`（含 `cacheRead`/`cacheWrite`/`tiers`）
38. `Model.compat` 的 45/48 个字段
39. `ThinkingLevel` 的 `"max"` 档
40. `ImagesModel` / `ImagesModels` 注册表 + 目录
41. 内置模型目录数据规模（39 provider vs 35 条）
42. `AssistantMessage.responseModel` / `responseId` / `providerThinkingLevel` / `diagnostics` / `endTurn`

**鉴权 / env（6）**
43. `ANTHROPIC_AUTH_TOKEN`（Bearer）
44. `ANTHROPIC_OAUTH_TOKEN`
45. provider 凭证优先级链
46. OAuth `radius`
47. `google-vertex` ADC 探测
48. `amazon-bedrock` 多凭证源

**usage（7）**
49. `usage.cacheRead` 生产者
50. `usage.cacheWrite` / `cacheWrite1h` 生产者
51. `usage.reasoning` 生产者
52. `usage.cost` 计算（`calculateCost`，`models.ts:891-911`）
53. `choice.usage` 回退（B24）
54. `parseChunkUsage` 归一（含 `prompt_tokens_details.cached_tokens` 等三路 + 减法语义）
55. `message_start` 首帧 usage 保留 + `message_delta` 逐字段覆盖（现在是 `orElse(0L)` 无条件覆盖）

**横切（8）**
56. `sanitizeSurrogates`（B16）
57. `transformMessages` 其余 4 条变换（B14；见 ContentBlock 32/34/35 + `thoughtSignature` 30）
58. Anthropic `cache_control` 标记
59. constrained sampling / grammar
60. 请求侧 `compat.thinkingFormat`（B21）
61. `simple-options`（`clampMaxTokensToContext` / thinking budget）
62. `transport`（websocket / websocket-cached）
63. `session-resources` 清理注册表
