/**
 * Empty module declaration for BOM aggregation.
 *
 * <p>The BOM module manages dependency versions and does not
 * contain Java code. This declaration exists only for project
 * completeness per the Phase 0 design.</p>
 */
module com.pijava.bom {
    // No exports — BOM artifact only manages dependency versions.
}
