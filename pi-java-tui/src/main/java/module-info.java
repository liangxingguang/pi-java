/**
 * Terminal UI for pi-java built on TamboUI.
 *
 * <p>Provides the interactive chat interface with panels for messages,
 * tool calls, diffs, and session browsing. Full implementation in Phase 3.</p>
 */
module com.pijava.tui {
    requires com.pijava.agent;
    // requires dev.tamboui.toolkit;      // Phase 3
    // requires dev.tamboui.panama.backend; // Phase 3

    // exports com.pijava.tui; — Phase 3
}
